export default function ParticipantsPage() {
  return <div>participants</div>;
}

{
  /* <div className='gap-6 grid sm:grid-cols-2 md:grid-cols-3 lg:grid-cols-4'>
  {users.map((user) => (
    <ProfileCard key={user.slug} user={user} />
  ))}
</div>; */
}
